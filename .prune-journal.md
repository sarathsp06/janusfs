# Prune journal

## 2026-07-27 - Removal audit: scaffolding traps vs genuine dead code

**Learning:**
`deadcode ./...` on this repo flags ~25 funcs, but MOST are NOT dead — they are
either platform-gated or live PRP scaffolding, and deleting them would break
queued work:
- `internal/procid/registry.go`, `isagent.go`, `environ_*`, `procid_darwin.go`
  → PRP 06 Tasks 1-2 done, Tasks 3-4 deferred pending PRP 07. Registry is built
  but not yet wired into the mount decision path. **Live scaffolding, keep.**
- `internal/nsexec/kernelversion.go`, `support_other.go` → PRP 04 (Linux
  namespace exec), unreachable on darwin only. **Platform code, keep.**
- `mount_darwin.go Adapter.Unmount`, `rules/glob.go compilePattern`,
  `patterns.go IsReserved`, `pidfile.go readPidfileMountpoint` → deadcode
  ignores test usage; these are **test-only**, not production-dead.

Genuinely dead (daemon architecture superseded standalone serving):
- `internal/api/server.go` `Listen`/`Serve`/`Shutdown` + `server` field +
  `withSecurity` no-op middleware. Production nests the api via `ServeHTTP`
  (daemon.go:553); the standalone-serve trio is never reached.

**Action:**
Before deleting any deadcode hit here, cross-check `PRPs/README.md` coverage
table + platform build tags. Only the api standalone-serve cluster and the
1.4MB `docs/janus_art.png` are clean high-value removals.

## 2026-07-27 - Feature-removal audit: the codebase is spec-locked

**Learning:**
`SPEC.md` is a formal FR/NFR contract and it pins essentially the ENTIRE
feature surface — there are almost no "features not required" to remove:
dashboard (FR-41/42/43, incl. config *write* + reveal → the vendored
CodeMirror is spec-justified), history/SQLite (§17 + FR-42 time-range panels +
FR-58 accepted risk), obs registry (FR-39/40), exec + Linux ns (FR-25–34),
process identity (FR-32), watchdog subprocess (FR-35; a *separate binary* is
explicitly a rejected design), virtual `.janusfs` files (FR-45). Every CLI
command has an FR (doctor 50, install 52, paths/path 51, check 48, explain 49).
Removing any = breaking the contract = out of prune's boundaries.

The ONE spec-sanctioned removal: §13 says the vestigial `watcherAlive` field
in status.json "should be removed" (FR-20 killed the watcher). Still present,
threaded through vfsmeta.go → api/server.go → mount/janus_virtual.go, always
false. Cleanest requirement-safe simplification available.

Queued, DO NOT preempt: `docs/superpowers/plans/2026-07-23-prometheus-only-obs.md`
already scopes trimming `/top`, `/latency`, and embedded op metrics — all steps
still `- [ ]` (unstarted). That is the intended obs simplification; leave it.

**Action:**
For "remove a feature" asks here, the honest answer is usually "the spec
requires it." Point at `watcherAlive`, the api standalone-serve dead cluster,
and `docs/janus_art.png` — not at any FR-backed feature.
