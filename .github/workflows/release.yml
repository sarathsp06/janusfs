name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  goreleaser:
    # Build on a standard Linux runner. Since CGO_ENABLED=0 is used,
    # goreleaser can cross-compile to all target platforms.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
